# Hygen generate command

This workspace project contains all templates to generate a boilerplate command. Just type the `name` separated by hyphen and press enter.

---

Generate command:

```shell
npm run generate
```
